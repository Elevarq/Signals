apiVersion: v2
name: arq-signals
description: Read-only PostgreSQL diagnostic collector
type: application
# Policy: chart `version`, `appVersion`, and `image.tag` in values.yaml
# are kept in lockstep with the released container image. Bump all three
# together at release time; do not float chart version independently.
version: 0.10.0-beta.3
appVersion: "0.10.0-beta.3"
keywords:
  - postgresql
  - monitoring
  - diagnostics
  - database
  - observability
home: https://github.com/elevarq/arq-signals
sources:
  - https://github.com/elevarq/arq-signals
