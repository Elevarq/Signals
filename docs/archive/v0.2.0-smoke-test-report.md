# v0.2.0 Release Smoke Test Report

Date: 2026-03-14

## Environments tested

| Instance | PG Version | Port | Database | Role | Mode |
|----------|-----------|------|----------|------|------|
| PG 18.1 | 18.1 | 54318 | frs | arq_signals | Safe (no override) |
| PG 16.11 | 16.11 | 54316 | csd_test | arq_signals | Safe (no override) |
| PG 14.20 | 14.20 | 54314 | postgres | postgres | Unsafe override |
| PG 15.15 | 15.15 | 54315 | postgres | postgres | Unsafe override |
| PG 16.11 | 16.11 | 54316 | postgres | postgres | Unsafe override |
| PG 18.1 | 18.1 | 54318 | postgres | postgres | Unsafe override |

## Scenario 1 — Production-style safe role

**Environment:** PG 18.1, database `frs`, role `arq_signals`

| Check | Result |
|-------|--------|
| Connection | PASS |
| Role safety checks | PASS (no override needed) |
| unsafe_mode in metadata | `false` |
| Collectors executed | 29/29 |
| Query failures | 0 |
| Snapshot created | PASS (255,246 bytes) |
| Export ZIP created | PASS (5 files, 562KB) |
| Metadata clean | PASS (no unsafe_reasons) |
| Credentials in export | PASS (only pg_settings parameter names) |
| Log warnings | None |

## Scenario 2 — Graceful degradation

**Environment:** PG 16.11, database `csd_test`, role `arq_signals`

| Check | Result |
|-------|--------|
| checkpointer_stats_v1 | SKIPPED (PG < 17, correct) |
| pgss_reset_check_v1 | Included (pg_stat_statements installed) |
| Replication collectors | Executed, returned empty (no replicas/slots — correct) |
| Eligible queries | 28 (29 minus checkpointer) |
| Query failures | 0 |
| Snapshot/export | PASS |

## Scenario 3 — Cross-version quick pass

| Version | Eligible | Executed | Errors | Notes |
|---------|----------|----------|--------|-------|
| PG 14.20 | 26 | 26 | 0 | checkpointer + pgss queries excluded (correct) |
| PG 15.15 | 26 | 26 | 0 | Same exclusions |
| PG 16.11 | 26 | 26 | 0 | Same exclusions |
| PG 18.1 | 29 | 29 | 0 | All collectors including checkpointer |

pg_stat_statements dynamic capture (`SELECT *`) worked correctly on
PG 18. PG 17+ checkpointer collector included on PG 18, excluded on
14/15/16.

## Scenario 4 — API / CLI path

**Environment:** PG 18.1, port 19001

| Step | Result |
|------|--------|
| `GET /health` | 200, `{"status":"ok","version":"dev"}` |
| `GET /status` (with token) | 200, 29 queries registered, 1 snapshot |
| /status secret fields | PASS — no secret_type, secret_ref, or password fields |
| Trigger collection | 202, `{"status":"collection triggered"}` |
| Export ZIP | 5 files, metadata + snapshots + catalog + runs + results |
| ZIP metadata | schema_version=arq-snapshot.v1, unsafe_mode=false |
| Credential scan of export | 2 matches (pg_settings parameter names only) |

## Automated tests

| Check | Result |
|-------|--------|
| `go vet` | OK |
| `go build` | OK |
| Tests | 135 pass, 0 fail |

## Failures found

**None.**

## Documentation mismatches found

**None.**

## Release verdict

**v0.2.0 is fit for public release.**

All four scenarios pass cleanly. The collector inventory, version
gating, graceful degradation, API contract, export format, credential
handling, and safety model all behave as documented. Cross-version
validation covers PG 14 through PG 18 with zero errors.
