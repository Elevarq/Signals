bin/
*.db
*.db-wal
*.db-shm
.env
test-config.yaml

# Root-level binaries produced by `go build ./cmd/...`. These take
# their names from the cmd/ subdirectories and would otherwise get
# staged accidentally when an operator builds from the repo root.
/signals
/signalsctl

# Editor / OS noise. Not tracked today but easily staged by accident.
.DS_Store
.idea/
.vscode/
*.swp
